export type RDividerVariant = "middle" | "inset" | "full-bleed"

/** Props for the {@link RDivider} component. */
export type RDividerProps = {
    /** Controls the divider's placement within its layout. */
    variant?: RDividerVariant
}
