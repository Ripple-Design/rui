/** Props for the {@link RWell} component. */
export type RWellProps = {
    /** Chooses which HTML element the well should render as. */
    as?: string
    /** Overrides the inherited content color; emphasized descendants continue using the on-background emphasis colors. */
    contentColor?: string
}
