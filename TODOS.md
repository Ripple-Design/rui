# TODOs

- [ ] Add Chip selection indicator motion: animate the leading check/icon transition and the resulting Chip width change when Filter/Input selection changes, while honoring `prefers-reduced-motion` and the project’s discrete CSS animation convention.
- [ ] Add TextField adornment motion: animate start/end icon and clear-action visibility changes while preserving the field layout, focus behavior, and the project’s discrete CSS animation convention.
- [ ] Investigate a cross-browser overlay scrollbar treatment: keep the thumb visible while preventing the scrollbar from consuming layout space on classic-scrollbar platforms.
- [ ] Investigate M2 App Bar lift elevation timing: compare the generic `280ms` elevation transition with Material Components Android's `150ms` app-bar state animator, then decide whether to add an app-bar-specific motion token or keep the generic transition. Reference `packages/rui/src/styles/elevations.scss`, `material-components-android/lib/java/com/google/android/material/appbar/res/values/integers.xml`, and `res/animator-v21/design_appbar_state_list_animator.xml`.
- [ ] Edge-to-edge App Bar support: implement an M2 content scrim layer behind toolbar content, an independent status-bar scrim layer, a configurable visible-height trigger with the M2 600ms timing and FastOutLinearIn/LinearOutSlowIn show/hide easing, and safe-area top inset handling in AppBar/Scaffold geometry and collapse range. Reference `packages/rui/src/components/appBar/RTopAppBar.vue`, `RAppBarContainer.vue`, `packages/rui/src/components/scaffold/RScaffold.vue`, `material-components-android/lib/java/com/google/android/material/appbar/CollapsingToolbarLayout.java`, `AppBarLayout.java`, and `res/values/attrs.xml`.
- [ ] Fix Checkbox and RadioButton label activation feedback: make the touch target cover the entire label, including slotted text, while a text click triggers Ripple only in the icon control area. This needs a label-level event host with the Ripple surface redirected to the control (rather than a control-local `.rui-touch-target`).
- [ ] Rework M2 Collapsing App Bar snap settling: the current visual-only snap can desynchronize the App Bar’s visible height from the native scrolling content, while mutating `scrollTop` inside the animation creates a scroll/cancel/reschedule feedback loop. Design a single coordinated offset model that preserves content position, supports scrollbar dragging without unwanted settle, and matches M2 edge selection, velocity timing, and 600ms cap. Reference `packages/rui/src/components/scaffold/RScaffold.vue` and `material-components-android/lib/java/com/google/android/material/appbar/AppBarLayout.java`.
- [ ] Investigate M2 Collapsing App Bar collapsed-title customization: decide whether `collapsedTitleGravity` and `collapsedTitleAppearance` should remain public APIs. Current M2-style composition delegates the visible collapsed title to the internal `RTopAppBar`, so custom collapsed gravity/appearance need a measured title-anchor and typography interpolation design rather than a second visual title layer. Reference `packages/rui/src/components/appBar/RCollapsingAppBar.vue`, `RTopAppBar.vue`, and `material-components-android/lib/java/com/google/android/material/appbar/CollapsingToolbarLayout.java`.
- [x] Won't implement expanded-title top-bound customization for the current single-line, bottom-aligned M2 title; revisit only if safe-area or multiline title support is introduced.
- [x] Improve Checkbox DotLottie visual sharpness: explicitly configure the renderer with the client DPR after mount, so its canvas buffer matches the CSS display size without SSR hydration mismatches.
- [ ] Research and improve the infinite scroll API
- [ ] Check whether the Data table implementation is correct

