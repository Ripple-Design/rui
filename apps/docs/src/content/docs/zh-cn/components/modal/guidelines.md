---
title: Modal
docSlug: components/modal
tab: guidelines
locale: zh-cn
designOrder: 80
developOrder: 80
api: /zh-cn/api/modal
---

## 使用建议

当你需要原生 `<dialog>` 行为、但不想直接使用 `RDialog` 预设的 title/content/actions 结构时，使用 `RModal`。

需要自定义视觉时，与 `RSurface` 组合使用。
